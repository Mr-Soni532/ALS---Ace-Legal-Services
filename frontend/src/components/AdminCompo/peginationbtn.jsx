// const button = ()=>{
//     return (
//     <div>
//         {Array.from({ length: totalPages }, (_, index) => (
//           <button key={index + 1} onClick={() => handlePageChange(index + 1)}>
//             {index + 1}
//           </button>
//         ))}
//     </div>
//       )
// }